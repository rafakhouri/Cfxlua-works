settings = {}

settings.seuNome = 'rafa'
settings.suaKey = 'dev'

settings.suaWebhook = 'https://discord.com/api/webhooks/1229479718894567580/gs71xOfz5xaNzmdFGEJu03Fk-XyjRjCX7RUX2jUy7Y1WVEu5VqCzoCz-Vkgu0TlbUSvl'

settings.admins = { -- Os IDS dos seus admins (precisa ser configurado 1 por 1, com virgula de 1 pro outro sempre)
    4
}

settings.policiais = { -- Os IDS dos policiais da sua cidade (existem maneiras mais fáceis de fazer sem colocar 1 por 1, me contate, é grátis)
    3
}

settings.rastreador = 2 -- De quantos em quantos segundos o rastreador da policia atualiza no carro do corredor

settings.autobanPolicia = false -- Se a policia tomará ban do painel automaticamente quando abrir

settings.precisaPagarParaIniciar = false -- Se precisa pagar para iniciar a corrida

settings.valorPorcorrida = 15000 -- Valor que o player receberá após concluir uma corrida

settings.pagamento = 2 -- 1 para vRP.giveMoney // 2 para vRP.giveBankMoney 

settings.tipoPagamento = 1 -- 1 para Dinheiro Limpo // 2 para Dinheiro Sujo 

settings.valorParainiciar = 5000 -- Quanto o player deverá pagar para iniciar a corrida

settings.dinheiroSujo = 'dinheirosujo' -- O nome do item "dinheiro sujo" na sua base, caso opte por usar o pagamento por dinheiro sujo

settings.precisaDeTablet = false -- Se precisa ou não do item tablet pra abrir o painel

settings.itemTablet = 'tablet' -- O nome do item tablet que você criou, ou seja, o item que o player precisa ter para abrir o painel.